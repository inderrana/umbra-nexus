apiVersion: v1
kind: Service
metadata:
  name: umbra-nexus-service
  labels:
    app: umbra-nexus
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: umbra-nexus
