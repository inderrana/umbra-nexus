# Environment Variables Configuration
# Copy this file to .env and update with your values

# Server Configuration
PORT=3000
NODE_ENV=production

# Security Configuration
ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com

# Admin email to receive notifications
ADMIN_EMAIL=your-email@example.com

# Optional: Email Service (SendGrid, Mailgun, etc.)
# SendGrid (if using email notifications)
SENDGRID_API_KEY=your-sendgrid-api-key
FROM_EMAIL=noreply@blah.com

# Optional: Database Configuration (if migrating from file storage)
# Supabase (if using database storage)
SUPABASE_URL=your-supabase-url
SUPABASE_KEY=your-supabase-key

# MongoDB (if using MongoDB)
MONGODB_URI=your-mongodb-connection-string

# Google Sheets (if using sheets)
GOOGLE_SHEETS_ID=your-sheet-id
GOOGLE_SERVICE_ACCOUNT=your-service-account-json

# Optional: reCAPTCHA (recommended for production)
# RECAPTCHA_SECRET_KEY=your_secret_key_here

